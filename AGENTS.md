# AGENTS.md

## 规范

1. 禁止修改非项目文件，只允许修改当前目录下的文件。
2. 禁止运行程序，代码编写完后仅允许编译检验。
3. 禁止直接安装任何软件或依赖，但可以向我提出安装申请。

## 工作流

1. 了解阶段：使用`ls`等命令查看当前项目结构。
2. 需求阶段：明确需求，包括输入输出等。若需求模糊，先向我提出澄清问题。
3. 分析阶段：分析需求是否合理，是否存在边界情况，是否有问题。如果有问题需先向我反馈。
4. 方案阶段：分析需求该如何实现，规划改动范围，包括文件，函数，框架等。待我确认后再生成代码。
5. 生成阶段：按照要求严格生成代码，禁止修改其它内容，且代码风格要和项目其它代码保持一致。
6. 整理阶段：如果有格式化工具，使用格式化工具格式化代码。
7. 检查阶段：生成后，自我审查是否遵循了上述所有规范，然后汇总归纳。

## 编码规范

1. 业务代码需在一个函数内编写完，禁止拆分为多个函数调用。
2. 统一使用单行注释，禁止使用多行注释，类和方法上必须有注释，注释必须使用中文。
3. 代码风格保持一致，注释风格保持一致，注释内容与代码一致，禁止使用全角标点。

## 文档规范

1. 用户不一定阅读过项目代码，文档必须保证没有基础知识的人也能直接看懂。
2. 文档必须自包含，用户只需阅读这一份文档即可了解内容，禁止在文档中让用户再查阅其它文档。
3. 必须列出变动的功能点名称，是什么，为什么，有什么作用。
4. 必须写清项目当前情况，修改后的情况，以及修改前后区别。
5. 必须声明项目改动会带来哪些风险，以及如何规避。
6. 必须列出具体出处（内容来源于哪些代码/依赖）
7. 考虑是否有遗漏情况
