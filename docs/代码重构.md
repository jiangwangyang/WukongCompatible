# 代码重构

> 本文档记录本项目（黑神话:悟空主题 Minecraft 模组，MC 1.20.1 + Forge + Epic Fight 20.14.17）的全部不改变运行行为的代码整理工作，按时间顺序整合自四份历史文档（原文件已删除，内容完整保留）：
> - 弃用API清理.md —— 弃用 API 分析与等价替换
> - 注释统一说明.md —— 全库注释风格统一
> - 代码清理分析报告.md —— 两轮冗余/死代码清理
> - 代码重构报告.md —— import 展开、成员顺序统一、注释整理、全量格式化与资源清理
>
> 共同原则：本档所录改动均为行为等价的整理（替换/移动/删除死代码/注释调整/格式化），不改变游戏内表现；功能性 bug 修复见姊妹篇 docs/代码修复.md。

## 总目录

| 章节 | 内容 | 原文档 |
|---|---|---|
| 第一部分 | 弃用 API 清理: 分析与替换 | 弃用API清理.md |
| 第二部分 | 项目代码注释统一 | 注释统一说明.md |
| 第三部分 | 冗余代码清理分析报告 | 代码清理分析报告.md |
| 第四部分 | 代码重构报告（含资源文件清理） | 代码重构报告.md |

---

# 第一部分: 弃用 API 清理（原 弃用API清理.md）

## 1. 这份文档讲什么

本项目(黑神话: 悟空连招 Epic Fight 附属, MC 1.20.1 + Forge 47.4.10 + Epic Fight 20.14.17)的代码中存在大量调用了"已被官方标记为弃用(Deprecated)的方法"的情况。弃用意味着: 官方(原版 Minecraft/Forge/Epic Fight)已经不推荐使用这些方法, 并在未来的版本中会删除它们, 继续积累会导致将来升级时大面积崩坏。

本次改动使用 javac 编译器自带的弃用警告分析(`-Xlint:deprecation`)对全部 93 个源文件做了扫描, 并把"当前依赖版本中存在官方替代 API"的弃用调用全部替换为等价的新 API; 对于"当前依赖版本中不存在替代 API"的少数弃用调用, 保留原样并添加说明注释。

改动结论: 编译弃用警告从 308 条降至 69 条, 剩余 69 条全部是依赖版本内无替代 API 的 Epic Fight 武器构建器方法(见第 5 节), 全部替换均为行为等价替换, 不改变任何游戏内表现。

## 2. 分析工具与方法

- 工具: JDK 17 javac 的 `-Xlint:deprecation` 警告(在 build.gradle 的 JavaCompile 任务中开启), 并调高 `-Xmaxwarns` 上限。
- 重要发现: javac 默认只报告前 100 条警告, 本项目实际警告 308 条, 首轮扫描曾被默认上限截断导致漏报(例如 FashuAnshenfaSkill 后半部分), 提高 `-Xmaxwarns` 后才获得完整清单。
- 每一种弃用 API 的替代方案, 都通过反编译依赖 jar(javap 查看字节码与注解)逐一核实后才动手替换, 替代依据在下一节逐条列出。

## 3. 变动功能点清单(是什么/为什么/有什么作用)

### 3.1 技能数据同步 setDataSync(改动最多, 共 171 处, 25 个文件)

- 是什么: `SkillDataManager.setDataSync(key, value, player)` 三参数重载已被 Epic Fight 标记弃用(forRemoval)。
- 替换为: 两参数的 `setDataSync(key, value)`。
- 为什么等价: 反编译比对证实, 弃用的三参版本内部就是 `setData(key, value)` + `syncServerPlayerData(key, player)`; 两参版本是 `setData(key, value)` 后按容器执行者自动分端: 服务端用容器自己的 ServerPlayer 同步, 客户端用容器自己的 LocalPlayer 同步。本项目中所有调用点传入的 player 本来就是该数据容器所属的同一个玩家, 因此两者完全等价, 两参版本还能自动区分客户端/服务端, 更不易误用。
- 涉及文件: WukongAnimations, WukongGreatSageAnimations, BattleUnit, SmashHeavyAttack, ThrustHeavyAttack, PillarHeavyAttack, GreatSageHeavyAttack, StaffPassive, WukongDodgeSkill, ShenfaJuxingsanqiSkill, ShenfaTongtoutiebiSkill, FashuAnshenfaSkill, FashuDingshenfaSkill, ShenWaiShenFaSkill, WukongKeyMappings 等。

### 3.2 资源路径 ResourceLocation 构造器(共 51 处, 21 个文件)

- 是什么: `new ResourceLocation(namespace, path)`(48 处)与 `new ResourceLocation(String)`(3 处)两个构造器已被原版标记弃用且 forRemoval。
- 替换为: 官方静态工厂方法 `ResourceLocation.fromNamespaceAndPath(ns, path)` 与 `ResourceLocation.parse(str)`。
- 为什么等价: 工厂方法在 1.20.1 官方 jar 中已存在(反编译核实), 内部校验逻辑与构造器一致; 三处单参调用(Config.java 实体名校验, FakeWukongRenderer 的 steve 贴图, StaffPassive 的实体类型解析)解析的字符串语义不变。
- 涉及文件: JinGuBangRenderer(7), ThrustHeavyAttack(7), SmashHeavyAttack(7), PillarHeavyAttack(7), GreatSageHeavyAttack(7), WukongWeaponCapabilityPresets, WukongSkillDataKeys, WuKongSounds, EpicFightDamageType, DaShengArmorTextureHandler, DaShengArmorRenderer, FakeWukongRenderer, Config, PacketHandler, WKCapabilityProvider, StaffPassive, 各 fashu 技能等。

### 3.3 主模组类构造方式(WukongMoveset.java)

- 是什么: 构造器里使用的 `FMLJavaModLoadingContext.get()` 与 `ModLoadingContext.get()` 已被 Forge 标记弃用, Forge 的 javadoc 明确写明替代方案是"在模组构造器中接收 context 参数"。
- 替换为: 构造器签名改为 `public WukongMoveset(FMLJavaModLoadingContext context)`。反编译 Forge 47.4.10 的 FMLModContainer 证实, 模组加载时会优先尝试 `getDeclaredConstructor(FMLJavaModLoadingContext.class)` 进行注入, 注入的实例与 `get()` 返回的是同一个对象, 因此 `context.getModEventBus()` 与 `context.registerConfig(...)` 行为完全一致。
- 作用: 消除加载路径上的弃用调用, 这是 Forge 1.21.1+ 的强制写法, 提前迁移。

### 3.4 客户端玩家补丁获取(CameraAnim.java, 2 处)

- 是什么: `ClientEngine.getInstance().getPlayerPatch()` 已被 Epic Fight 标记弃用。
- 替换为: `EpicFightCapabilities.getEntityPatch(Minecraft.getInstance().player, LocalPlayerPatch.class)`。反编译证实弃用方法的内部实现正是这一行(且 getEntityPatch 对 null 玩家返回 null, 与原方法一致, 原有的判空逻辑保留)。
- 作用: 镜头拉近功能(蓄力时第三人称视角偏移)改用官方推荐的通用补丁获取入口。

### 3.5 粒子注册(WuKongParticles.java, 2 处)

- 是什么: `ParticleEngine.register(type, provider)` 已被 Forge 标记弃用。
- 替换为: 同一个 `RegisterParticleProvidersEvent` 事件自带的 `event.registerSpriteSet(type, provider)`(该类本来就在监听此事件)。两者接收同一个函数式接口 `SpriteParticleRegistration`, 方法引用 `DingParticle.DangerParticleProvider::new` 原样保留。
- 作用: 粒子提供者注册改为 Forge 推荐的事件 API。

### 3.6 玩家移动输入拦截(SmashHeavyAttack, PillarHeavyAttack, StaffPassive, 共 4 处)

- 是什么: Epic Fight 事件 `MovementInputEvent.getMovementInput()`(返回可变的原版 Input 对象)已弃用, 新 API 为不可变的 `PlayerInputState` 记录类。
- 替换为: `InputManager.setInputState(event.getInputState().withXxx(...))` 链式写法。等价性依据: 反编译 `InputManager.setInputState` 证实其内部调用 `PlayerInputState.applyToVanillaInput(state, Minecraft.getInstance().player.input)`, 会把全部 8 个字段(leftImpulse/forwardImpulse/up/down/left/right/jumping/sneaking)写回原版 Input, 其中 sneaking 对应原 Input.shiftKeyDown; Epic Fight 自己的新版技能(SteelWhirlwindSkill, GuardSkill)正是这种写法。
- 作用: 蓄力期间禁止跳跃, 棍花期间禁止移动/潜行等输入压制逻辑迁移到新输入 API。

### 3.7 按键状态设置(StaffPassive, PillarHeavyAttack, 共 2 处)

- 是什么: `ControlEngine.setKeyBind(keyMapping, value)` 已被 Epic Fight 标记弃用且类内无其他重载。
- 替换为: 直接调用 `keyMapping.setDown(value)`。反编译证实弃用方法的方法体就只有这一行委托调用, 行为逐字节一致。作用: 棍花期间强制关闭疾跑。

### 3.8 盔甲材料修复原料懒加载(WukongArmorMaterials.java)

- 是什么: 原版工具类 `net.minecraft.util.LazyLoadedValue` 整个类已被标记弃用。
- 替换为: Forge 的 `net.minecraftforge.common.util.Lazy`(通过 `Lazy.of(supplier)` 创建)。两者都是"首次 get() 时才执行 supplier"的懒加载包装, `get()` 调用点代码不变; 修复原料 `Ingredient.of(Items.NETHERITE_INGOT)` 的注册时机不变。

### 3.9 粒子贴图集常量(ParticleRenderTypeN.java, 1 处)

- 是什么: `TextureAtlas.LOCATION_PARTICLES` 已被原版标记弃用, 1.20.1 中不存在替代常量(替代常量 AtlasIds 是 1.20.5 才加入的)。
- 替换为: 按反编译核实的常量值 `minecraft:textures/atlas/particles.png` 等价内联为 `ResourceLocation.withDefaultNamespace("textures/atlas/particles.png")`。
- 作用: 自定义粒子渲染类型(立棍式技能特效)的贴图集引用改用非弃用 API, 值完全相同。

### 3.10 分析能力固化(build.gradle)

- 在 JavaCompile 任务中新增 `options.compilerArgs << '-Xlint:deprecation'` 与 `-Xmaxwarns 100000`。
- 作用: 以后每次编译都会把所有弃用 API 的使用位置完整打印出来, 不再受 javac 默认 100 条上限的截断, 可持续监控技术债。

## 4. 修改前后对比

- 修改前: 编译产生 308 条弃用警告(其中因 javac 默认上限, 常规编译只显示 100 条), 涉及 33 个源文件, 18 种弃用 API; 模组功能不受影响但弃用面持续扩大。
- 修改后: 编译产生 69 条弃用警告, 全部集中在 WukongWeaponCapabilityPresets.java 一个文件的 6 种 Epic Fight 武器构建器方法上(见第 5 节); 其余 17 种弃用 API 清零。
- 行为变化: 无。所有替换都经过反编译比对, 是调用形态的等价改写, 游戏内表现(技能, 输入, 渲染, 注册)不变。

## 5. 保留未替换的 69 条警告(依赖版本内无替代 API)

WukongWeaponCapabilityPresets.java 中 `WeaponCapability$Builder` 的 6 个方法: livingMotionModifier(49 处), innateSkill(7 处), newStyleCombo(7 处), styleProvider(2 处), comboCancel(2 处), passiveSkill(2 处)。

保留原因(均已反编译 Epic Fight 20.14.17 官方 jar 核实):

1. 这 6 个方法在当前依赖版本中各自只有一个重载, 不存在任何非弃用的替代方法;
2. 弃用注解标注 `since = "1.21.1", forRemoval = true`, 即替代 API 只存在于 Epic Fight 的 1.21.1+ 代码库, 1.20.1 版本根本没有;
3. Epic Fight 官方自带的 WeaponCapabilityPresets 与 JSON 加载器 WeaponTypeReloadListener 同样在调用这些方法(仅官方预设中 livingMotionModifier 就调用了 89 次), 它们就是 1.20.1 下的标准用法;
4. 唯一的替换途径是升级 Epic Fight 到 1.21.1 版本, 意味着放弃 MC 1.20.1 支持, 属于独立的依赖升级工程, 不在本项目当前目标内。

处理方式: 在 WukongWeaponCapabilityPresets.java 类头添加了说明注释, 保留编译警告作为将来升级时的迁移检查清单, 未添加 @SuppressWarnings(按约定)。

## 6. 风险与规避

1. 替换引入行为差异的风险: 低。每类替换都通过反编译核实了内部实现等价( setDataSync 的同步目标, getEntityPatch 的空值保护, setKeyBind 的委托实现, applyToVanillaInput 的字段映射, Lazy 的懒加载语义, fromNamespaceAndPath/parse 的校验逻辑)。规避方式: 建议实际运行游戏验证核心路径: 蓄力/棍花/闪避/法术的技能数据同步(服务端与客户端数值应保持一致), 蓄力镜头, 棍花禁移动与禁疾跑, 粒子与贴图显示, 盔甲修复配方。
2. WukongMoveset 构造器签名变更的风险: 低。模组类仅由 FML 反射构造, FMLModContainer 会自动尝试带 FMLJavaModLoadingContext 参数的构造器(反编译核实); 项目内无其他 new WukongMoveset() 调用点。规避方式: 启动游戏确认模组正常加载, 配置文件正常生成。
3. 批量脚本误伤的风险: 替换由脚本辅助完成, 曾出现注释结构与行尾重复两类脚本缺陷, 均已通过 git 还原并修复脚本后重做; 最终以 `git show HEAD` 比对确认所有被改文件的注释标记数量不变, 并以 javac 全量编译通过(0 错误)兜底。
4. 遗漏风险: 弃用警告清单已提高 -Xmaxwarns 后完整收集(308 条全覆盖), 不存在因上限截断的漏报; 剩余 69 条为有意保留。后续新代码若引入弃用 API, 编译时会被 -Xlint:deprecation 直接暴露。

## 7. 依据来源

- 弃用清单: javac -Xlint:deprecation 对本项目 93 个源文件的编译输出(修改前 308 条, 修改后 69 条)。
- Epic Fight 侧结论: 反编译 build/fg_cache 与 forge_gradle 缓存中的 EpicFight-20.14.17_mapped_official_1.20.1.jar, 涉及 SkillDataManager, WeaponCapability$Builder, ClientEngine, MovementInputEvent, PlayerInputState, InputManager, ControlEngine, EpicFightCapabilities, WeaponCapabilityPresets, WeaponTypeReloadListener 等类的字节码与 @Deprecated 注解。
- Forge/Minecraft 侧结论: 反编译 forge-1.20.1-47.4.10_mapped_official_1.20.1-recomp.jar(ResourceLocation, TextureAtlas, LazyLoadedValue, RegisterParticleProvidersEvent, Lazy)与 javafmllanguage/fmlcore 1.20.1-47.4.10 的官方源码 jar(FMLJavaModLoadingContext, FMLModContainer, ModLoadingContext, 其中含弃用 javadoc 指明的替代方式)。
- 注: 本项目自带 build/fg_cache 编译缓存, 上述 jar 均为项目编译实际使用的 classpath 产物, 无需额外下载。

## 8. 遗漏情况说明

- 代码中被注释掉的死代码块(如各技能 onRemoved 中 /* */ 包裹的旧 setDataSync 调用)未做替换: 它们不参与编译, 保留原样以最小化改动; 若将来启用这些代码块, 需按第 3.1 节同步改为两参 setDataSync。(后续更新: 这些注释死代码已在本文第三部分的两轮清理中删除。)
- GeckoLib 依赖(4.8.3)未扫描出本项目代码对它的弃用调用, 无需处理。
- 非代码文件(资源 json, mixins 配置)不涉及弃用 API。
- staff_stack 相关 GUI 贴图路径等字符串拼接调用, 替换后字符串内容未改动, 已由编译与常量值核对覆盖。

---

# 第二部分: 项目代码注释统一（原 注释统一说明.md）

## 一、本次做了什么

本次改动只做了一件事: 为项目的全部代码文件统一注释风格, 没有修改任何一行代码逻辑。

涉及范围:
- src/main/java/com/p1nero/wukong 下的全部 89 个 Java 文件
- tools/convert_dasheng_armor.py (1 个 Python 工具脚本)

合计 90 个文件。

## 二、为什么这么做

项目原有的注释情况参差不齐, 存在三类问题:

1. 一部分文件完全没有注释, 阅读时不知道类和方法的用途 (例如 WukongStyles.java, WukongColliders.java, 大部分 entity 和 item 类)。
2. 一部分文件使用 /** ... */ 块注释 (javadoc 形式), 与项目编码规范要求的单行注释 // 不一致。
3. 少量注释中使用了全角标点 (如全角逗号, 全角括号), 与编码规范要求的半角标点不一致; 还有少量注释是英文或存在错别字, 与项目其余注释不统一。

统一注释后, 任何一个没有读过项目代码的人都可以通过注释快速了解每个类是做什么的, 每个方法做什么事。

## 三、统一后的注释规范

本次统一严格遵循 AGENTS.md 中的编码规范, 最终所有文件达到以下标准:

| 规范项 | 统一后的标准 |
|---|---|
| 注释形式 | 只使用单行注释 // (Python 使用 #), 全项目已无任何 /* */ 或 /** */ 块注释 |
| 注释语言 | 全部简体中文, 原有英文注释已翻译为中文 |
| 标点 | 注释内一律半角 ASCII 标点, 全角标点已清零 |
| 类注释 | 每个类 (含枚举/接口/内部类) 都有注释说明用途 |
| 方法注释 | 每个方法都有注释说明功能 |
| 字段/逻辑注释 | 关键字段和复杂逻辑段落补充了说明 |
| 原有信息 | 原有 // TODO 注释, 注释掉的调试代码全部原样保留 |

## 四、修改前后的区别

修改前:
- 部分类和方法没有任何注释, 需要通读代码才能理解用途
- 注释形式混杂: /** */ 块注释与 // 单行注释并存
- 注释语言和标点不统一: 中英文混用, 全角半角混用

修改后:
- 全部 89 个 Java 文件和 1 个 Python 脚本的每个类和方法都有中文注释, 关键字段与复杂逻辑段有补充说明
- 全项目注释统一为 // (Python 为 #) 单行形式, 无块注释残留
- 注释全部为中文半角标点, 风格一致

## 五、功能点清单 (均为注释层面的变动, 不涉及任何功能)

| 变动点 | 是什么 | 为什么 | 作用 |
|---|---|---|---|
| 补全缺失注释 | 为原先无注释的约 40 个文件补上类/方法/字段注释 | 原文件无注释难以阅读 | 提升可读性, 方便维护 |
| 块注释转单行注释 | 将 /** */ 改写为等价的 // | 与编码规范统一 | 全项目注释形式一致 |
| 全角标点规范化 | 注释中的全角逗号/括号/引号等改为半角 | 与编码规范统一 | 注释风格一致 |
| 英文注释中文化 | 将少量英文注释翻译为等价中文 | 统一注释语言 | 中文读者直接可读 |
| 注释纠错 | 修正少量错别字与复制粘贴错误的注释 (如 "是否正在切手技" 误写为 "是否正在切手技" 之外的错文, 伤害减免被误标为防止坠机等) | 注释与代码实际行为不符会误导读者 | 保证注释内容准确 |

## 六、改动来源 (具体出处)

- 注释规范的依据: 项目根目录 AGENTS.md 的 "编码规范" 一节 (统一单行注释, 中文注释, 类和方法必须有注释)。
- 改动涉及的代码: src/main/java/com/p1nero/wukong 下的 89 个 Java 文件 (capability, client, effects, entity, epicfight, item, listener, mixin, network, Config, WukongMoveset) 以及 tools/convert_dasheng_armor.py。

## 七、风险评估与规避方式

| 风险 | 说明 | 规避方式 | 实际验证结果 |
|---|---|---|---|
| 误改代码逻辑 | 批量修改注释时可能误删或改动代码行 | 逐文件修改后用 grep 全量复查, 确认除注释外无任何改动 | 已复查, 无代码行变动 |
| 编译失败 | 修改可能导致语法错误 | 已执行编译检验 (gradlew compileJava) | 编译通过 (exit 0) |
| 遗留警告 | 编译输出有 69 个警告 | 逐条核对 | 全部为 epicfight 依赖库 API 的弃用 (deprecation) 警告, 属项目原有情况, 与本次注释改动无关, 修改前后完全一致 |

## 八、是否有遗漏

- 已覆盖: src 下全部 89 个 Java 文件, tools 下 1 个 Python 脚本。
- 不涉及: 资源文件 (.json/.ogg/.toml 等) 和 build 目录不属于代码, 无需注释。
- 两处含义不完全明确的命名已在注释中如实按代码行为描述, 未臆测业务含义:
  - WukongMoveCoordFunctions 中的 SJZT 标志 (注释为 "一次性视角俯仰角调整标志")
  - JinGuBangRenderer 中的 MAX_CZLT (注释按实际行为描述: 贴图档位索引随发光动画循环递增)

如你了解这两个命名的准确业务含义, 可以告诉我, 我会把注释更新为更准确的描述。

---

# 第三部分: 冗余代码清理分析报告（原 代码清理分析报告.md）

> 适用范围：`src/main/java/com/p1nero/wukong/` 全部 Java 源码与 `src/main/resources` 资源。
> 状态：**清理已完成**。本文档记录两轮清理的结果：第一轮（提交 `c4e81db`）删除了报告初版列出的大部分死代码；第二轮（本次）统一复核全库后，清除了第一轮遗留项与新发现的死代码。
> 对读者的说明：本项目是一个《黑神话:悟空》主题的 Minecraft 模组，基于 Epic Fight 战斗框架实现劈棍/立棍/戳棍/大圣四套棍式技能。技能动画在 Java 代码中以"动画访问器"（`AnimationAccessor` 静态字段）声明，再通过 `nextAccessor(...)` 注册后才有实际内容；**只声明不注册的字段永远是 null，任何对它的调用都会引发空指针崩溃**。

---

## 一、本轮（第二轮）清理明细

### 1.1 已声明但永不赋值的动画访问器（空指针隐患）

| 是什么 | 为什么删 | 出处 |
|---|---|---|
| `WukongAnimations.SMASH_CHARGING_LOOP` 声明 | 只声明未注册，恒为 null；唯一引用点在 `WukongSkills.java` 劈棍技能的 `setChargingAnimation(...)`，而 `SmashHeavyAttack.Builder.chargingAnimation` 字段注释自述"占位, 当前未使用"，从未调用。实际劈棍蓄力循环用的是已注册的 `SMASH_CHARGING_LOOP_STAND`。属未引爆的空指针雷，连带删除声明、引用、占位字段与 setter | `WukongAnimations.java`、`WukongSkills.java`、`SmashHeavyAttack.java` Builder |
| `WukongAnimations.PILLAR_UP4` 声明 | 同上：恒为 null，作为 `setDeriveAnimations` 第二参数传入，对应 `PillarHeavyAttack.Builder.deriveLoop`（注释自述"未使用"），构造方法从不读取。连带删除声明、引用、`deriveLoop` 字段与 `setDeriveAnimations` 的对应参数 | `WukongAnimations.java`、`WukongSkills.java`、`PillarHeavyAttack.java` Builder |
| `PILLAR_CHARGED_LOOP1/2/3`、`PILLAR_CHARGED_LOOP0TOP1/1TOP2/2TOP3` 共 6 个声明 | 第一轮删除旧"立棍蓄力循环"注释注册块后遗留的空声明，全库无任何引用。保留仍在注册使用的 `PILLAR_CHARGED_LOOP4` 与 `PILLAR_LOOP0` | `WukongAnimations.java` |
| `WukongGreatSageAnimations.FENG_YUN_SAO_STYLE` / `SAO_CHUO_SHI_STYLE` 别名字段 | 赋值为 `WIND_CLOUD_STYLE` / `SWEEP_JAB_STYLE` 的别名，但全库从未读取 | `WukongGreatSageAnimations.java` |

### 1.2 永不成立的死分支

| 是什么 | 为什么删 | 出处 |
|---|---|---|
| `WKPlayer.damageReduce` 字段与 `getDamageReduce()` | 字段私有且全库没有任何赋值入口，恒为 0；`PillarHeavyAttack` 受击监听里 `if (wkPlayer.getDamageReduce() > 0)` 整块（约 30 行免伤逻辑）永远不执行。注意：劈棍/立棍/大圣受击监听中的局部变量 `damageReduce` 读的是 `WukongSkillDataKeys.DAMAGE_REDUCE`（四蓄霸体减伤，活跃功能），与此无关，**未动** | `capability/WKPlayer.java`、`PillarHeavyAttack.java` |
| `ShenfaJuxingsanqiSkill.currentWeapon` 字段与 `restoreArmor(...)` 方法 | 字段从未赋值，方法从未被调用（疑似"聚形散气卸甲复原"未完成功能的残留）。连带删除仅被该方法使用的 `InteractionHand`/`EquipmentSlot`/`ItemStack` 三个 import | `epicfight/skill/custom/fashu/ShenfaJuxingsanqiSkill.java` |
| `Config.registerCommands`（`/wukong` 空命令壳） | 注册了一个没有任何子命令、没有执行体的空命令，玩家输入无效。连带删除 4 个仅被它使用的 import 并更新类注释 | `Config.java` |

### 1.3 从未被读取的 Builder 字段

| 是什么 | 为什么删 | 出处 |
|---|---|---|
| 四个法术技能 Builder 的 `animationProviders` 字段与 `setAnimations(...)` 方法 | 聚形散气/安身法/铜头铁臂/定身法四个技能：字段仅被自己的 setter 赋值，而 setter 在 `WukongSkills` 注册处从未被调用，构造方法也从不读取。法术技能动画已全部改走事件触发 | `ShenfaJuxingsanqiSkill`、`FashuAnshenfaSkill`、`ShenfaTongtoutiebiSkill`、`FashuDingshenfaSkill` |
| `ThrustHeavyAttack.Builder.chargingAnimation` 与 setter | 与 1.1 劈棍同款占位字段，戳棍注册处甚至连 setter 都从未调用 | `ThrustHeavyAttack.java` |

### 1.4 与父类完全相同的空 override（9 处）

- **是什么**：5 个法术技能与 4 个 HeavyAttack 技能各自 override 了 `registerPropertiesToAnimation()`，方法体只有 `return this;`。
- **为什么删**：已反编译验证 Epic Fight 20.14.17 基类 `Skill.registerPropertiesToAnimation()` 的字节码就是 `aload_0; areturn`（即 `return this;`），`WeaponInnateSkill` 未再 override，删除后行为逐字节一致。
- **出处**：`ShenfaJuxingsanqiSkill`、`FashuAnshenfaSkill`、`ShenfaTongtoutiebiSkill`、`FashuDingshenfaSkill`、`ShenWaiShenFaSkill`、`ThrustHeavyAttack`、`SmashHeavyAttack`、`GreatSageHeavyAttack`、`PillarHeavyAttack`；基类证据来自 `libs/EpicFight-20.14.17.jar` 的 `yesman/epicfight/skill/Skill.class`（javap 验证）。

### 1.5 笔误与调试残留

| 是什么 | 出处 |
|---|---|
| `System.out.println("man!")` 调试输出（劈棍防摔落分支） | `SmashHeavyAttack.java` |
| 连续重复两行的 `event.setCanceled(true)` 共 4 处（幂等无害，疑似复制粘贴笔误），各删一行 | `SmashHeavyAttack.java` 两处、`PillarHeavyAttack.java` 一处、`ShenfaTongtoutiebiSkill.java` 一处 |
| 事件监听 lambda 体内一个多余的空语句 `;` | `ShenWaiShenFaSkill.java` |

### 1.6 孤儿资源

- **是什么**：`assets/wukong/animmodels/animations/biped/staff_flower/` 目录下两个动画 json（单手/双手棍花）。
- **为什么删**：全库（Java/语言文件/配置）无任何引用；游戏内"棍花"实际播放的是 `biped/staff_spin/*`（由 `PlayStaffFlowerPacket` 播放 `STAFF_SPIN_ONE_HAND_LOOP` / `STAFF_SPIN_TWO_HAND_LOOP`，见 `StaffPassive.java` 与按键 `WukongKeyMappings.STAFF_FLOWER`）。属迁移遗留的无效文件。

---

## 二、第一轮（提交 `c4e81db` 及后续提交）已完成的清理

以下为报告初版列出、并在本轮复核中确认**已不存在**的条目，不再赘述细节：被注释的调试代码与旧逻辑共 15 组（含 `WukongAnimations` 105 行旧立棍蓄力注释块、`ThrustHeavyAttack` 旧法术栈逻辑、5 个法术技能旧重置逻辑、`MoreBattlesSkillDataKeys` 迁移残留等）；空方法/空类 6 处（含广智两个空技能类、`AnimationPlayerMixin` 死注入点及其在 `wukong.mixins.json` 的条目）；未使用符号 10 项（`EVISCERATE_FIRST`、`heavyAttackKeyPressed`、`WKPlayer.blfov` 系、`Config.setConfig` 私有重载等）；`Config.DERIVE_STAMINA_CONSUME` 与 `staff_flower_stamina_consume` 两个体力配置项（提交 `b540565`）；乱码注释、重复 `putBoolean` 等小缺陷。

---

## 三、复核后确认"保留/无害"的项（避免误删）

- **`biped/*/data/` 目录下的 json**（如 `thrust/data/thrust_pre.json`、`smash/data/smash_charging.json`）：内容是刀光拖尾特效（`trail_effects`）与动画层（layer/masks）元数据，不是动画本体，代码本就不按 `data/` 路径注册动画，属正常设计，**保留**。
- **`StaffPassive`、`ThrustHeavyAttack` 中分散在不同 if 分支里的单个 `event.setCanceled(true)`**：分属雷伤免疫/棍花格挡/防摔落/退寸等不同分支，不是重复笔误，**保留**。
- **设计性空 override**（`CloudStepLeftEntity.setItemSlot` 等防复制保护、Epic Fight 生命周期钩子）、**所有 mixin 类**（被 `wukong.mixins.json` 引用）：**保留**。
- **`WukongWeaponCapabilityPresets` 的 `livingMotionModifier` 弃用 API 警告**（编译期 69 个警告的主要来源）：Epic Fight 标记待删除但仍可用的 API，替换需要逐动画验证表现，超出本轮范围，**保留**，编译警告不影响运行。

---

## 四、修改前后对比

- **修改前**：存在 2 个可被未来代码误用即空指针的 null 动画访问器、6 个注册块已删的空声明、1 个恒为 0 的 capability 字段及其约 30 行永不执行的免伤分支、4+2 个从未被读取的 Builder 占位字段、9 个与父类逐字节相同的空 override、1 个空调试输出、4 处重复 `setCanceled`、1 个空命令壳、2 个孤儿动画资源。
- **修改后**：上述全部移除，代码与资源共 **16 个文件，删除 217 行、插入 2 行（注释改写），净减 215 行**；`./gradlew compileJava` 构建成功。
- **行为区别**：无任何运行时行为变化。被删代码全部为"从未执行"或"与父类相同"的代码；所有活跃功能（四蓄霸体减伤 `WukongSkillDataKeys.DAMAGE_REDUCE`、棍花 `staff_spin`、劈棍蓄力循环 `SMASH_CHARGING_LOOP_STAND` 等）均未改动。

## 五、风险与规避

1. **误删风险**：已用全库文本检索交叉验证每个被删符号"零读取/零赋值"，并通过编译验证；动画访问器类资源由路径字符串驱动，删除 Java 字段不影响资源 JSON。
2. **存档兼容**：`WKPlayer.damageReduce` 从未写入 NBT（`saveNBTData` 只存 `lastSkill`），删除不影响旧存档。
3. **配置兼容**：本轮未动任何 Forge 配置项，`config/wukong-common.toml` 无需处理。
4. **命令兼容**：`/wukong` 原为空壳，无任何子命令与执行体，删除后玩家输入会提示"未知命令"，无功能损失。
5. **后续维护**：若未来要恢复"蓄力循环动画"或"衍生长循环"功能，应在 `WukongAnimations` 中重新 `nextAccessor` 注册后再引用，切勿直接引用未注册字段（这正是本轮清除的隐患模式）。

## 六、出处

- 本轮修改的 16 个文件见 `git diff`（13 个 Java 文件 + 1 个目录下 2 个资源 json + 本文档）。
- Epic Fight 基类行为证据：`libs/EpicFight-20.14.17.jar`（javap 反汇编 `Skill.registerPropertiesToAnimation`）。
- 初版分析条目与第一轮清理的对应关系见本文档第二节及提交 `c4e81db`、`b540565`。
- 活跃功能未动的对照证据：`WukongSkillDataKeys.DAMAGE_REDUCE`（Smash/Pillar/GreatSage 受击监听读取）、`PlayStaffFlowerPacket`（棍花播放 staff_spin）、`WukongAnimations.java` 中 `SMASH_CHARGING_LOOP_STAND` 的注册点。

## 七、已知遗留（明确不处理）

- `livingMotionModifier` 等弃用 API 的 69 个编译警告（见第三节；本文第一部分的结论是当前依赖版本内无替代 API，有意保留作为将来升级 Epic Fight 时的迁移检查清单）。
- `biped/` 顶层 `data/` 目录与各棍式 `data/` 目录的特效 json 均未注册引用，属特效数据存放约定，非死代码。

---

# 第四部分: 代码重构报告（原 代码重构报告.md）

> 适用范围：`src/main/java/com/p1nero/wukong/` 全部 90 个 Java 源文件（约 18100 行）。
> 本次重构共修改 46 个文件，净删除约 81 行（+350 / -431）。未改动 `src/main/resources` 任何资源。
> 项目背景说明：本项目是《黑神话:悟空》主题的 Minecraft Forge 模组（MC 1.20.1），基于 Epic Fight 20.14.17 战斗框架实现劈棍/立棍/戳棍/大圣四套棍式技能。阅读本报告无需了解项目代码。

---

## 一、项目当前情况（修改前）

项目此前已经历两轮死代码清理（见本文第三部分），大块死代码已清除，但仍存在四类问题：

1. **风格不统一**：约 20 个文件使用通配符 import（如 `import yesman.epicfight.skill.*;`），其余文件使用显式 import，两种风格混存；3 个文件用全限定名 `new java.util.ArrayList<>(...)` 而非 import；部分文件类注释放置在类注解之后。
2. **顺序不统一**：四个重击技能类中，`getHeavyAttacks()` 重写方法有的排在构造方法之前、有的之后；`StaffStance` 的静态工厂方法夹在构造方法与重写方法之间；两个粒子类把内部类放在构造方法之前；`WukongAnimations` 把两个静态辅助方法放在 80 个静态字段之前。
3. **残留无用代码**：全库无任何调用的死方法/死字段 8 处；`AnimationJudge.isCharging()` 中同一条件被完全重复判断了两遍；注释掉的调试日志与旧实现残留 11 处。
4. **注释问题**：3 处 TODO/FIXME 陈旧标记；若干注释与代码行错位（注释描述的是下一行的内容）；若干注释仅复述代码字面含义；1 处英文注释（规范要求中文）；3 个类与 9 个方法缺少注释。

## 二、修改后的情况与逐项变动

统一执行的约定：**成员顺序 = 静态字段 → 实例字段 → 静态块 → 静态工厂方法 → 构造方法 → 其余方法（保持原相对顺序）→ 内部类/Builder；类注释在类注解之前；import 全部显式展开；注释为中文单行且与所描述的代码对应。**

### 2.1 通配符 import 展开（20 个文件）

- **是什么**：把 `import 包.*;` 全部替换为实际使用类的显式 import，并删除与同包显式 import 的重复行。
- **为什么**：显式 import 能直接看出每个类的外部依赖，避免同名类歧义，是项目多数文件已有的风格。
- **作用**：纯声明层面整理，不改变任何运行行为。
- **出处**：`WukongSkillDataKeys`、`WukongSkills`、5 个法术技能、5 个棍式技能、`WukongAnimations`、`WukongItems`、`DaShengArmorItem`、`WKCapabilityProvider`、`BasicAttackMixin`、`CloudStepLeftEntity`、`FakeWukongEntity`、`DingParticle`、`DingEntityAfterImageParticle` 等。包内容清单来自 `libs/EpicFight-20.14.17.jar` 与 `forge-1.20.1-47.4.16_mapped_parchment_2023.09.03-1.20.1.jar`。

### 2.2 删除无用代码（8 处）

| 删除项 | 是什么 | 为什么确认无用 | 出处 |
|---|---|---|---|
| `WukongAnimations.applyKnockback(...)` | 水平击退工具方法 | 全库无任何调用 | `epicfight/animation/WukongAnimations.java` |
| `WukongAnimations.CameraOperationFov(...)` | 后台线程 FOV 平滑调整方法 | 全库无调用；FOV 功能已由 `client/event/PillarFovController.java` 接管 | 同上 |
| `WukongAnimations.addItemEffectTimer(...)` 与 `onPlayerTick(...)` | 向物品 NBT 写入/每 tick 递减特效计时 | 写入的 NBT 键 `wukong_has_effect_timer` 全库（含 resources）无任何读取方，属只写不读的写死数据；且每 tick 写物品 NBT 正是上一提交修复的 Boss 循环开场招问题的同类隐患 | `WukongAnimations.java`、`WukongMoveset.java`（连带删除 `ITEM_HAS_EFFECT_TIMER_KEY` 常量与 `fg_bus.addListener(WukongAnimations::onPlayerTick)` 注册） |
| `BattleUnit.fenshen(...)` | 环形生成分身的旧方法 | 全库无调用；身外身法分身实际由 `ShenWaiShenFaSkill` 自行生成 | `epicfight/skill/custom/BattleUnit.java` |
| `BattleUnit()` 空构造器 | 全静态工具类的公开空构造 | 无任何实例化 | 同上 |
| `EntitySpeedData.getOriginalSpeed(...)` | 读取原始移速的方法 | 全库无调用（只存/恢复两个方法在用） | `epicfight/skill/EntitySpeedData.java` |
| `PillarHeavyAttack.MAX_ANGLE_FOV` / `MAX_FOVLJ` | 两个 FOV 常量 | 全库无引用（`PillarFovController` 自行计算） | `epicfight/skill/custom/wukong/PillarHeavyAttack.java` |
| `AnimationJudge.ZERO_STOR` | 0 星动画列表 | 只构建、无任何 is* 判定方法使用 | `client/AnimationJudge.java` |
| `CameraAnim.zoomIn(Vec3f)` 单参重载 | 无保持时长的拉近重载 | 全库无调用（唯一调用点用双参版本） | `client/event/CameraAnim.java` |
| `WukongChargedAttackAnimation.hurtCollidingEntities(...)` 空 override | 方法体仅原样调用父类同名方法 | 删除后行为与父类逐字节一致（与上轮清理的 9 处空 override 同类） | `epicfight/animation/custom/WukongChargedAttackAnimation.java` |

### 2.3 逻辑等价简化（1 处，不改变行为）

- **是什么**：`AnimationJudge.isCharging()` 原实现把 `IS_CHARGING` 键的 `hasData && getDataValue` 判断完全重复写了两遍，且 6 次重复调用 `lpp.getSkill(SkillSlots.WEAPON_INNATE)`。
- **修改后**：先取一次技能容器并判空，再用 `SkillDataManager` 局部变量分别判断劈棍/戳棍两个蓄力键，或运算返回。逻辑与原式完全等价（重复析取项不改变真值）。
- **出处**：`client/AnimationJudge.java`。

### 2.4 成员顺序统一（9 个文件）

| 文件 | 调整内容 |
|---|---|
| `SmashHeavyAttack` / `PillarHeavyAttack` / `ThrustHeavyAttack` | `getHeavyAttacks()` 从构造方法前移到构造方法后（与 `GreatSageHeavyAttack` 对齐，后者反向把该校验辅助方法之后的方法前移对齐） |
| `GreatSageHeavyAttack` | `getHeavyAttacks()` 移到构造方法之后、私有校验辅助方法之前 |
| `StaffStance` | 静态工厂 `createStaffStyle()` 移到构造方法之前 |
| `WukongWeaponCategories` | 字段 `id` 移到构造方法之前 |
| `JinGuBangRenderer` | 静态字段集中排前，实例字段在后 |
| `DingParticle` / `DingEntityAfterImageParticle` | 内部类 `DangerParticleProvider` 移到类末尾 |
| `WukongAnimations` | 两个静态辅助方法从 80 个静态字段之前移到方法区；`ScaleTime` record 保持末尾内部类型位置 |

以上均为纯代码块位置移动，未修改任何语句。

### 2.5 注释整理

- **删除注释掉的死代码 11 处**：被注释的调试日志（`LOGGER.info`）6 处（`ShenfaJuxingsanqiSkill`、`FashuAnshenfaSkill`、`ThrustHeavyAttack`×2、`PillarHeavyAttack`、残留）；`WukongMoveset.onPlayerLoggedIn` 中 2 行调试残留；`WukongDodgeSkill` 中被注释的发包语句；`SmashHeavyAttack` 中被注释的 `playSound` 及其空 `if` 外壳。
- **处理 3 处 TODO/FIXME**：`CameraAnim` 的 `// TODO int InsuranceTime`（连同无人调用的单参重载一并删除）；`SmashHeavyAttack` 的 `// 防止坠机 FIXME` 改为与代码一致的正式描述；`WukongChargedAttackAnimation` 的 `// TODO` 连同空 override 一并删除。
- **修正错位注释**：`ShenfaJuxingsanqiSkill` 探测实体段 3 处行尾注释均描述的是下一行内容，已归位；`ItemRendererMixin` 的「获取物品的能力」注释从物品栏判断处移到真正的能力获取处。
- **删除复述代码的注释**：`BattleUnit`（获取玩家位置/设置位置）、`DaShengArmorItem`（获取服务器世界实例）、`DingEntityAfterImageParticle`（设置白色不透明等 7 处）、`WukongAnimations`（设置播放速度）。
- **口语化注释改写**：「放里面, 防止瞎按技能键就防坠机的bug」「算是保险」「// 保险」「手动加残影, 不知道为何...」等改为正式且含义不变的中文描述。
- **英文注释翻译**：`PillarFovController` 的英文注释译为中文（规范要求注释使用中文）。
- **补充缺失注释**：`WKCapabilityProvider`、`WKPlayer`、`FakeWukongEntityPatch` 3 个类补类注释；`PillarFovController` 全类字段/方法补注释；`MyKeyMapping`、`WuKongParticles`、`PacketRelay`、`WukongKeyMappings.registerKeys` 等 9 处方法/字段补注释。
- **注解与类注释顺序统一**：`WukongMoveset`、`DingParticle`、`DingEntityAfterImageParticle`、`WuKongParticles`、`WukongWeaponCapabilityPresets`、`WukongSkills`、`WukongAnimations`、`WukongGreatSageAnimations`、`Config`、`WukongEntities` 统一为类注释在前、类注解在后。
- 两个残影数据包文件头 3 行「不知道为何...无效」的吐槽注释压缩为一句说明性注释（保留「直接发粒子无效、故手动发包」这一关键信息）。

### 2.6 全量格式化

- 全部 90 个文件经 `tools/google-java-format-1.28.0-all-deps.jar --aosp`（4 空格缩进）格式化，与 `tools/README.md` 记载的项目约定一致；格式化器同时自动清除了因本次删改而不再使用的 import（如 `WukongAnimations` 的 `Minecraft`/`Executors` 等）。
- 格式化校验：`--dry-run --set-exit-if-changed` 全量通过。

## 三、修改前后区别一览

| 维度 | 修改前 | 修改后 |
|---|---|---|
| import 风格 | 通配符/显式混存，另有 3 处全限定名内联 | 全部显式 import，按字典序排列 |
| 成员顺序 | 工厂方法/重写方法/内部类位置各异 | 统一为 字段→静态块→工厂→构造→方法→内部类 |
| 死代码 | 8 处无调用方的方法/字段/常量 + 1 处重复条件 + 11 处注释死代码 | 全部清除 |
| 注释 | 错位/复述/口语化/英文/缺失并存 | 中文单行、与代码对应、类与方法均有注释 |
| 行为 | — | 不变（唯一例外见风险 1） |

## 四、风险与规避

1. **物品 NBT 特效计时机制的移除**：`onPlayerTick` 每 tick 递减 `wukong_has_effect_timer` NBT，删除后该 NBT 不再更新。经全库（Java 与 resources）检索确认没有任何读取方，属只写数据，删除无功能影响；反而消除了「每 tick 写 NBT 导致服务端误判装备变化」的同类隐患（参见提交 `ad0ce50` 的修复说明）。**规避方式**：若未来需要物品特效计时，应改用 `StaffScaleState` 式的客户端缓存，不要写物品 NBT。
2. **公开方法/常量的删除**（`applyKnockback`、`CameraOperationFov`、`MAX_ANGLE_FOV` 等）：理论上影响依赖本模组的外部代码，但本项目是独立模组而非库，且已全库检索确认无调用。**规避方式**：如需回滚，单项均在 git 历史中可查。
3. **大面积 diff 的审阅成本**：46 个文件变动。所有改动均可归类为「import 展开 / 位置移动 / 删除死代码 / 注释调整 / 格式化」五种无行为变更的类型，且 `./gradlew compileJava` 编译通过。
4. **未运行游戏实测**：按项目规范仅做编译检验，未启动游戏。技能连招、粒子、渲染等运行时行为虽未被代码改动触及，仍建议提交后常规进游戏抽查劈棍/立棍/戳棍/大圣四式重击与定身、聚形散气两个法术。

## 五、出处

- 代码出处均在各节表格中列出，路径相对于 `src/main/java/com/p1nero/wukong/`。
- 依赖证据：`libs/EpicFight-20.14.17.jar`（Epic Fight 20.14.17，`yesman.epicfight.skill` 等包的类清单）、`~/.gradle/caches/forge_gradle/.../forge-1.20.1-47.4.16_mapped_parchment_2023.09.03-1.20.1.jar`（`net.minecraft.*`、`net.minecraftforge.*` 包的类清单）。
- 格式化约定：`tools/README.md`（google-java-format 1.28.0 AOSP 风格）。
- 前序清理记录：本文第一、二、三部分。

## 六、遗漏情况说明

- `WuKongSounds.java` 中「字段组 + 静态块填充列表」按音效类别分组的写法刻意保留：静态初始化有顺序依赖，且文件内部分组方式本身一致。
- 各 `public` 字段/方法的命名（如 `MAX_CZLT`、`GH`、`CUNTUI_JIESUO`）未改动：改名属于 API 变更，超出本次重构范围。
- `record` 数据包类中静态 `decode` 位于两个重写方法之间：五个数据包类均为此布局，本身一致，未强行按「静态方法靠前」重排。
- 编译器弃用警告（EpicFight 20.14.17 的弃用 API）按 `WukongWeaponCapabilityPresets.java` 头注释的决定保留现状，不在本次范围内。

---

## 七、资源文件清理（第二版提交）

对 `src/main/resources` 全部文件逐一与 Java 代码、json 内部引用、`mods.toml` 交叉核对后，删除确认无用的孤儿文件 41 个（309 → 268）。

### 7.1 删除明细

| 删除项 | 判定依据 | 出处 |
|---|---|---|
| `textures/item/phoenixcrown/phoenixcrown.png` | 凤翅紫金冠贴图，对应物品不存在，全库无引用 | - |
| `textures/item/jingubang/jingubang.png`、`jingubang111.png`(+`.mcmeta`) | `JinGuBangRenderer.getTextureLocation` 只返回 jingubang0~4/6 号贴图 | `item/client/JinGuBangRenderer.java` |
| `textures/item/staff/staff.png` | staff 模型引用的是同目录 `texture.png` | `models/item/staff.json` |
| `particles/after_image_with_texture.json`（空 json）、`dangers2.json`、`emitlight.json`、`gold_glow.json` | 对应粒子类型从未注册（`WuKongParticles` 只注册 ding/shen），粒子 json 只有注册后才会被加载 | `client/particle/WuKongParticles.java` |
| `textures/particle/` 下 `gold_glow.png`、`staff.png`、`staff2.png`、`staffg1-4.png`、`stafft1.png`、`stafft3.png`、`stafft4.png` | 仅 `staff1.png`/`stafft2.png` 被两个 item_skins 拖尾引用，其余无引用 | `item_skins/staff.json`、`item_skins/jingubang.json` |
| `textures/mob_effect/emitlight.png`、`gold_glow.png`、`shen.png` | 药水效果图标按注册名自动加载，而 `WuKongEffect` 只注册了 ding | `client/particle/WuKongEffect.java` |
| `textures/gui/skills/` 下棍式/闪避图标与全部子目录（`dodge.png`、`dodge/`、`greatsage_style.png`、`pillar_style.png`、`smash_style.png`、`thrust_style.png`、`fashu_style/`、`hao_mao/`、`shenfa_style/`、`staff_style/`） | 旧版目录布局遗留；代码只引用顶层 `spell_*.png` 与 `staff_stack/` 目录 | 各技能 `drawOnGui` 方法 |
| `textures/gui/staff_stack/stance/{0_1,1_1,2_1}.png` | 棍式图标路径硬编码 `"_0.png"` 后缀，`_1` 变体从不使用 | 四个重击技能的 `drawOnGui` |
| `textures/particle/staff.aseprite` | Aseprite 美术工程源文件，运行时不加载 | - |
| `src/main/scale.py`（第一版已删） | 动画变换旧工具脚本，全库无引用 | - |

### 7.2 审计确认有用并保留的类别

`sounds.json` 与 34 个 ogg（每个 ogg 均被引用、每个事件均已注册）；93 个顶层动画 json（全部被 `nextAccessor` 引用，无缺失无多余）；`animmodels/armor/`（EpicFight 按注册名约定加载）；7 个物品模型及对应贴图；GeckoLib geo/animation；2 个 item_skins；3 个武器能力 json（物品 id 均已注册）；5 个 skill_parameters（与技能注册名一一对应）；`staff_stack/` HUD 贴图（progress 0~40、stack1~4、ui0~4、light 3 个、stance `*_0`，与代码动态拼接范围完全吻合，stack=0 时代码不绘制对应贴图）；5 个顶层法术图标；`lang/` 双语文件；`cover.png`（mods.toml logoFile）；`pack.mcmeta`/`mods.toml`/`wukong.mixins.json`/`accesstransformer.cfg`。

### 7.3 刻意保留

- `animmodels/animations/**/data/` 下 23 个动画 json：无任何访问器引用，运行时被扫描加载但从不播放，属动画制作工具链的原始导出件。经确认**保留**作为动画源文件，便于日后用工具链重新导出/修改动画。

### 7.4 风险

- 删除的贴图/粒子均无代码或 json 引用，不影响运行；若日后恢复旧版技能栏布局或新增效果，可从 git 历史找回。
- 资源删除不影响编译（`compileJava` 已通过）；建议进游戏抽查技能栏图标、棍子贴图与拖尾特效显示正常。
