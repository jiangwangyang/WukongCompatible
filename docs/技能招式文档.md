# 技能/招式文档(黑神话悟空动作模组)

## 0. 文档说明

本文档面向所有读者(无需阅读过代码), 完整列出本模组(Epic Fight 附属, 黑神话悟空棍法动作)中**每一个技能/招式的触发方式、触发效果及具体数值**。

阅读本文即可了解全部招式体系, 无需查阅其它文档。文中所有数值均直接来源于代码, 出处见文末第 9 节。

### 0.1 基础概念(先读懂这 3 个词)

| 概念 | 说明 |
| --- | --- |
| 棍势条 | 武器固有技能(重击)的资源条, **上限 120 点**。普攻命中、蓄力、格挡等都会积攒棍势。 |
| 棍势星(段) | 棍势条攒到 30%/50%/70%(即 36/60/84 点)时各 +1 星, 攒满 120 点为第 4 星(四蓄), **最多 4 星**。升星时多余棍势保留给下一段(4 星封顶 120, 无下一段)。释放重击/切手技消耗星数。 |
| 切手技(衍生) | 普攻后短暂窗口内按重击键使出的派生招式, 通常消耗 1 星。窗口默认为 **30 tick(1.5 秒)**。 |

> 单位说明: 20 tick = 1 秒。伤害一栏为**武器面板攻击力的倍率**(例如 2.6 表示 2.6 倍面板)。"硬直"指 Epic Fight 的 StunType: NONE(无)/HOLD(定住)/SHORT/LONG(长硬直)/KNOCKDOWN(击倒)/NEUTRALIZE(打断霸体)。

> 表格字段说明(全文固定): 第 1~7 章**招式表**统一为 9 列: `招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果`; 第 8 章**法术表**统一为 6 列: `技能 | 触发方式 | 冷却 | 持续/窗口 | 核心效果 | 附加说明`。无该项内容的格子统一填 `-`, 便于逐列横向对比。

### 0.2 默认按键一览

| 按键 | 功能 | 出处 |
| --- | --- | --- |
| 鼠标左键 | 轻攻击(普攻连段) | `WukongKeyMappings.JIAO_ZHEN` |
| 武器固有技能键(Epic Fight 键位, 代码注释中为右键, 可在设置中查看) | 重击/蓄力/切手技 | `EpicFightKeyMappings.WEAPON_INNATE_SKILL` |
| 闪避键(Epic Fight 键位, 默认 Left Alt) | 悟空闪避 | `WukongDodgeSkill` |
| Z / X / C / G | 切换 劈棍式 / 立棍式 / 戳棍式 / 大圣式 | `WukongKeyMappings` |
| 方向键 上 | 身法槽技能(聚形散气/铜头铁臂) | `WukongKeyMappings.MAGICARTS_SHENFA` |
| 方向键 下 | 法术槽技能(安身法/定身法) | `WukongKeyMappings.MAGICARTS_FASHU` |
| 方向键 左 | 毫毛槽技能(身外身法) | `WukongKeyMappings.MAGICARTS_HAOMAO` |
| V(按住) | 棍花 | `WukongKeyMappings.STAFF_FLOWER` |

> 前提条件: 以上招式仅在**战斗模式(Epic Fight 模式)且主手持有棍类武器**(棍/亢金/金箍棒, 分类 `wk_staff`)时可用。技能无需学习, 持棍时自动装配(`WukongKeyMappings.HandleClientTick` 自动 `addLearnedSkill`)。

### 0.3 全局通用数值(配置文件 Config 默认值, 可改)

| 参数 | 默认值 | 含义 |
| --- | --- | --- |
| charging_speed | 1.0 | 四蓄衰减速度基数(每 tick 流失 charging_speed/5 = 0.2 棍势); 注意: 蓄力与各类回棍势数值已改为固定值, 不再随该配置联动 |
| derive_check_time | 30 tick(1.5s) | 普攻后接切手技的判定窗口 |
| charged4_window_ticks | 300 tick(15s) | 四蓄窗口: 命中敌人即刷新计时(任意星级), 3 星以上(含满 4 星)窗口内不掉棍势, 窗口结束满 4 星降回 3 星 |
| basic_attack_time_interval | 30 tick(1.5s) | 自定义普攻连段间隔判定(比原版短) |
| 棍势条上限 | 120 点 | 各重击技能 `skill_parameters/*.json` 中 `consumption: 120, max_stacks: 4` |
| 四蓄衰减 | 0.2 / tick(=charging_speed 1.0/5) | 15 秒未命中敌人(窗口结束)且 3 星以上时, 棍势每 tick 流失量; 命中敌人即刷新窗口并停止流失 |
| 破条升星阈值 | 36 / 60 / 84 点(30%/50%/70%), 第 4 段攒满 120 | 棍势条每跨过一个阈值 +1 星, 多余棍势保留给下一段(四式统一) |

> **体力说明**: 本模组所有招式均**不消耗也不回复体力**(Epic Fight 耐力), 技能施放只受棍势/冷却约束, 体力条可视为与本模组无关。

---

## 1. 轻攻击(普攻连段)

**触发**: 持棍战斗模式下按攻击键(鼠标左键)连打, 共 5 段 + 冲刺攻击 + 跳跃轻击。劈棍/戳棍/立棍三式共用同一套普攻动作(立棍式多插一段), 大圣式使用独立动作。

### 1.1 劈棍/戳棍/立棍普攻(动画 `STAFF_AUTO1~5` 等)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 普攻 1 段 | 左键 | - | 0.9 | 默认 | 默认 | 默认 | 命中回复(固有技能机制) | - |
| 普攻 2 段 | 连打 | - | 1.25 | 默认 | 默认 | 默认 | 命中回复(固有技能机制) | - |
| 普攻 3 段 | 连打 | - | 1.0 x 2 次 | 默认 | 每段最多 4 个 | 默认 | 命中回复(固有技能机制) | - |
| 普攻 4 段 | 连打 | - | 0.5 x 4 次 + 收尾 1.0 | 收尾 x5 | 默认 | 收尾 KNOCKDOWN(击倒) | 命中回复(固有技能机制) | - |
| 普攻 5 段 | 连打 | - | 3.0 | x2 | 默认 | LONG | 命中回复(固有技能机制) | 动作期间 50% 减伤(霸体) |
| 冲刺攻击 | 冲刺中左键 | - | 0.9 | 默认 | 默认 | 默认 | 命中回复(固有技能机制) | - |
| 跳跃轻击 | 空中左键 | - | 1.45 | 默认 | 最多 1 个 | 默认 | 命中回复(固有技能机制) | 命中后自动接续一段下落攻击动画 |

### 1.2 大圣式普攻(动画 `greatsage/basic/auto_1~5`)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 普攻 1 段 | 左键 | 大圣式 | 1.0 | 默认 | 默认 | 默认 | 命中回复(固有技能机制) | - |
| 普攻 2 段 | 连打 | - | 1.0 | 默认 | 默认 | 默认 | 命中回复(固有技能机制) | - |
| 普攻 3 段 | 连打 | - | 2.0 + 1.0 | 默认 | 每段最多 4 个 | 默认 | 命中回复(固有技能机制) | - |
| 普攻 4 段 | 连打 | - | 3.0 + 0.5 x 3 + 收尾 1.0 | 收尾 x5 | 默认 | 收尾 KNOCKDOWN(击倒) | 命中回复(固有技能机制) | - |
| 普攻 5 段 | 连打 | - | 3.0 | x2 | 默认 | LONG | 命中回复(固有技能机制) | 全程追踪目标, 期间 50% 减伤并免疫下一次摔落伤害 |
| 冲刺攻击 | 冲刺中左键 | - | 1.25 | 默认 | 默认 | 默认 | 命中回复(固有技能机制) | - |

> 普攻是积攒棍势的主要手段(Epic Fight 武器固有技能机制: 命中回复资源)。普攻第 1~4 段后 30 tick 内按重击键可触发各棍式的切手技(见下)。

---

## 2. 悟空闪避(含完美闪避)

**触发**: 战斗模式持棍时按闪避键(默认 Left Alt), 按移动方向播放前/后/左/右闪避动画。**不消耗任何资源**。空中/水中/攀爬/骑乘时不可用。

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 悟空闪避 | 闪避键(默认 Left Alt), 方向由移动方向决定 | 战斗模式持棍; 空中/水中/攀爬/骑乘时不可用; 不消耗任何资源 | - | - | - | - | - | 连续闪避在 1→2→3 段动画间轮播, 100 tick(5s)不闪避后回到第 1 段; 闪避前摇期间带 Epic Fight 闪避帧(可闪掉攻击); 闪避时模型朝向按移动方向与镜头角度计算 |
| 完美闪避 | 闪避帧恰好躲过攻击(触发 `DODGE_SUCCESS_EVENT`) | 不消耗任何资源 | - | - | - | - | **+30 点** | 播放完美闪避音效 + 人物残影(发包 `AddEntityAfterImageParticle`), 并播放对应方向的完美闪避动画; **蓄力期间完美闪避可保留棍势**, 非完美闪避则清空棍势条与星数(出处 `WukongDodgeAnimation`) |

> 该技能无需学习: 持棍时被动技能"棍花"会自动把闪避槽替换为悟空闪避, 收棍后还原原闪避(`StaffPassive`)。

---

## 3. 棍花(武器被动技能)

**触发**: 地面持棍时长按 **V**; 按住 V 的同时按 **W** 变为双手棍花。**不消耗体力**。棍花期间禁止移动/跳跃/疾跑。

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 棍花格挡 | 地面持棍时长按 V(按住 V + W 为双手棍花) | 不消耗体力; 棍花期间禁止移动/跳跃/疾跑 | - | - | - | - | 格挡成功 **+5 点** | 可格挡**正面**(视线方向点积 > 0)的可格挡实体伤害; 默认可格挡列表仅 `minecraft:arrow`(可在配置 `可被棍花格挡的实体` 中添加), 以及生物弹射物伤害; 格挡成功时伤害完全取消(判定 BLOCKED), 播放金属碰撞音与火花粒子 |
| 棍花命中敌人 | 棍花旋转中碰到敌人 | - | - | - | - | - | **+5 点 / 次**(故意设计为回的比打的多) | - |
| 雷击免疫 | 棍花连段(普攻 4 段)动画期间 | - | - | - | - | - | - | 免疫雷电伤害 |

> 出处: `StaffPassive` + `Config.ENTITIES_CAN_BE_BLOCKED_BY_STAFF_FLOWER`。

---

## 4. 劈棍式(默认棍式, 按 Z 切换)

重击键(武器固有技能键)按下后的判定顺序: 空中→跳跃重击; 切手技窗口内且有星→破棍式/斩棍式; 满 4 星且穿全套大圣套装→三段衍生(变身); 否则进入蓄力。蓄力期间**禁止跳跃**, 做其它动作会清空棍势。

### 4.1 蓄力重击(按住重击键蓄力, 松手释放)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 蓄力重击(0 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 2.6 | x2.0 | 默认 | LONG | 释放后清空 | - |
| 蓄力重击(1 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 5.6 | x2.5 | 默认 | 默认 | 释放后清空 | - |
| 蓄力重击(2 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 8.8 | 默认 | 4 | 默认 | 释放后清空 | 释放期间 **50%** 减伤(霸体) |
| 蓄力重击(3 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 11.0 | x3.5 | 6 | NEUTRALIZE | 释放后清空 | 释放期间 **70%** 减伤(霸体) |
| 蓄力重击(4 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 15.5 | x4.0 | 默认 x2 | NEUTRALIZE | 释放后清空 | 释放期间 **90%** 减伤(霸体) |

蓄力速度 1.5 棍势/tick, 蓄到 3 星后不再继续自动积攒(第 4 星需把条攒满)。

### 4.2 切手技: 破棍式(一段衍生)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 破棍式(一段衍生) | 普攻 1~4 段后 30 tick(1.5s)内按重击键 | 消耗 1 星 | 0 星时 x1.0; 有星时 **x1.96** | x4.0 | 默认 | HOLD | **识破(See Through)**: 动作全程处于识破状态, 此时被击中则完全免伤并 **+60 棍势**(仅奖励一次) | 识破成功后下次普攻直接从第 3 段开始; 命中敌人后解锁二段衍生窗口(30 tick) |

### 4.3 切手技: 斩棍式(二段衍生)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 斩棍式(0 星) | 破棍式命中后 30 tick 内再按重击键 | 消耗 1 星 | 4.48 | x3.0 | 默认 | LONG | - | - |
| 斩棍式(1 星) | 破棍式命中后 30 tick 内再按重击键 | 消耗 1 星 | 4.7 | x3.0 | 默认 | LONG | - | - |
| 斩棍式(2 星) | 破棍式命中后 30 tick 内再按重击键 | 消耗 1 星 | 4.9 | x3.0 | 默认 | LONG | - | - |
| 斩棍式(3/4 星) | 破棍式命中后 30 tick 内再按重击键 | 消耗 1 星 | 5.1 | x3.0 | 默认 | LONG | - | - |

### 4.4 跳跃重击

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 跳跃重击(0 星) | 空中按重击键 | 消耗所有星数并免疫本次落地摔伤 | 1.45 | x2.0 | 默认 | LONG | 释放后清空 | - |
| 跳跃重击(1 星) | 空中按重击键 | 消耗所有星数并免疫本次落地摔伤 | 3.0 | x2.0 | 默认 | LONG | 释放后清空 | - |
| 跳跃重击(2 星) | 空中按重击键 | 消耗所有星数并免疫本次落地摔伤 | 4.5 | x2.0 | 默认 | LONG | 释放后清空 | - |
| 跳跃重击(3 星) | 空中按重击键 | 消耗所有星数并免疫本次落地摔伤 | 6.2 | x2.0 | 默认 | LONG | 释放后清空 | - |
| 跳跃重击(4 星) | 空中按重击键 | 消耗所有星数并免疫本次落地摔伤 | 8.75 | x2.0 | 默认 | LONG | 释放后清空 | - |

### 4.5 三段衍生(大圣套装专属)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 三段衍生(变身) | 满 4 星时按重击键 | **穿戴全套大圣套装**(头/胸/腿/脚) | 0.9 | 默认 | 默认 | 默认 | - | 播放变身动画(HENSHIN) |

> 通用补充: 重击/衍生释放后免疫下一次摔落伤害(防"坠机"); 对已经倒地的敌人不施加硬直。

---

## 5. 立棍式(按 X 切换)

重击键判定: 满 4 星→直接 4 星起手; 铜头铁臂格挡成功窗口内→直接起手; 切手技窗口内且有星→风云转; 江海翻窗口内→江海翻; 否则开始立棍蓄力(需头顶 星数x2 格空间为空气, 空间不足无法起棍)。蓄力期间**禁止移动与跳跃**, 并有镜头 FOV 拉远动画(+7, 31 tick, 按星数重复)。

### 5.1 蓄力重击(立棍爬升后砸下)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 蓄力重击(0 星) | 立棍蓄力爬升后砸下 | 需头顶 星数x2 格空间为空气, 空间不足无法起棍; 释放消耗所有星数 | 1.75 | 默认 | 默认 | 默认 | 释放后清空 | 升星时播放"棍子变长"动画(蓄力中每升 1 星棍身变长一次) |
| 蓄力重击(1 星) | 立棍蓄力爬升后砸下 | 同上 | 3.75 | 默认 | 默认 | 默认 | 释放后清空 | 同上 |
| 蓄力重击(2 星) | 立棍蓄力爬升后砸下 | 同上 | 5.75 | 默认 | 默认 | 默认 | 释放后清空 | 同上 |
| 蓄力重击(3 星) | 立棍蓄力爬升后砸下 | 同上 | 7.75 | 默认 | 默认 | 默认 | 释放后清空 | 同上 |
| 蓄力重击(4 星) | 立棍蓄力爬升后砸下 | 同上 | 11.0 | x2.5 | 默认 | 默认 | 释放后清空 | 同上 |

### 5.2 切手技: 风云转(一段衍生)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 风云转(一段衍生) | 普攻 1~4 段后 30 tick 内按重击键; 长按重击键持续旋转, 松手播放收尾 | 消耗 1 星 | 4 段旋转, 每段 **x1.12** | 每段 x4.0 | 默认 | 默认 | 每次命中 **+5 棍势**, 4 星后不再回 | - |

### 5.3 切手技: 江海翻(二段衍生)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 江海翻 第 1 段 | 风云转后(江海翻计时器窗口内)按重击键 | 消耗 1 星 | 0.9 | 默认 | 默认 | 默认 | - | - |
| 江海翻 第 2 段(大范围) | 接第 1 段自动接续 | - | 4.48 | 默认 | 默认 | 默认 | - | - |

> 立棍同样有霸体减伤(动画标记 50%/70%/90%, 机制与劈棍一致, 见 4.1)与摔落保护; 戳棍的凤穿花命中还带击退(见 6.4 注)。

---

## 6. 戳棍式(按 C 切换)

重击键判定: 满 4 星→直接释放**凤穿花**(无蓄力前摇); 退寸窗口内→退寸; 二段窗口内→进尺; 否则进入蓄力。

### 6.1 蓄力重击

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 蓄力重击(0 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 2.0 | 默认 | 默认 | LONG | 释放后清空 | - |
| 蓄力重击(1 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 4.0 | 默认 | 默认 | 默认 | 释放后清空 | - |
| 蓄力重击(2 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 6.25 | 默认 | 默认 | 默认 | 释放后清空 | - |
| 蓄力重击(3 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 8.0 | x50 | 默认 | 默认 | 释放后清空 | 命中时目标击退 1.5 格强度 + **点燃 10 秒** |

### 6.2 切手技: 退寸(后撤步)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 退寸(后撤步) | 普攻 1~6 段后 30 tick 内按重击键 | - | - | - | - | - | 赌胜(退寸期间被击中)**+30 棍势** | 赌胜时**完全免伤**, 视作完美闪避: 播放完美闪避音效与残影; 退寸后 30 tick 内: 按住攻击键→搅棍, 再按重击键→进尺 |

### 6.3 切手技: 搅棍(长按攻击键持续突刺)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 搅棍(长按持续突刺) | 退寸后 30 tick 内**按住攻击键**进入, 松开攻击键自动收尾 | - | 起手 x1.68; 循环每轮 4 段 x0.672 + 1 段 x0.9 | 默认 | 默认 | 默认 | 每次命中 **+10 棍势** | - |

### 6.4 切手技: 进尺(收棍突进)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 进尺(收棍突进) | 退寸后 30 tick 内再按重击键 | 消耗 1 星(0 星时退化为 0 星重击) | **x3.92** | x50 | 默认 | 默认 | - | 命中时目标击退 1.0 格强度 + **点燃 10 秒** |

### 6.5 四星直接释放: 凤穿花

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 凤穿花(四星直接释放) | 满 4 星时按重击键直接释放(无需蓄力) | 消耗全部棍势 | **x15.6** | x50 | 锁定为 50 | 默认 | 释放后清空 | - |

> 戳棍 3 星蓄力重击/进尺命中时, 击退强度按与目标的距离归一化后施加, 并点燃目标 10 秒(出处 `ThrustHeavyAttack.createRepelForAttackTarget`)。

---

## 7. 大圣式(按 G 切换)

融合四式: 普攻 1~4 段后 30 tick(1.5s)内按重击键触发一段衍生(按普攻段数分支), 一段衍生后 30 tick(1.5s)内再按重击键消耗 1 星触发二段衍生; 否则进入蓄力。第 4 段普攻后的一段衍生会进入**立棍蓄力**(上棍)。

### 7.1 一段衍生(免费, 不耗星)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 断棍式(大圣破棍) | 普攻 1 段后 30 tick(1.5s)内按重击键 | 免费, 不耗星 | 1.25 | 默认 | 默认 | 默认 | **识破**成功 **+60 棍势** | **识破**: 动作期间被击中完全免伤, 且普攻重置到第 1 段衍生 |
| 扫戳式 | 普攻 2 段后 30 tick(1.5s)内按重击键 | 免费, 不耗星 | 0.5 + 1.0(两段) | 默认 | 默认 | 默认 | - | 第 2 段使用大型碰撞体 |
| 风云式 | 普攻 3 段后 30 tick(1.5s)内按重击键 | 免费, 不耗星 | 1.5 | 默认 | 默认 | 默认 | - | - |
| 立棍起手 | 普攻 4 段后 30 tick(1.5s)内按重击键 | 免费, 不耗星 | 见 5.1 | - | - | - | - | 转为立棍蓄力(上棍), 最高蓄到 3 星 |

### 7.2 二段衍生(一段后 30 tick(1.5s)内, 消耗 1 星)

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 劈棍式(斩) | 普攻 1 段或 2 段路线的一段衍生后 30 tick(1.5s)内再按重击键 | 消耗 1 星 | 5.0 | 默认 | 默认 | 默认 | - | - |
| 江海翻 | 普攻 3 段路线的一段衍生后 30 tick(1.5s)内再按重击键 | 消耗 1 星 | 0.9 + 4.48 | 默认 | 默认 | 默认 | - | - |
| 连段重击 | 立棍路线的一段衍生后 30 tick(1.5s)内再按重击键 | 消耗 1 星 | 0.9 + 1.48 + 4.48 | 默认 | 默认 | 默认 | - | - |

### 7.3 蓄力重击

| 招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 蓄力重击(0 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 2.6 | 默认 | 默认 | 默认 | 释放后清空 | 与劈棍 0 星相同 |
| 蓄力重击(1 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 5.6 | 默认 | 默认 | 默认 | 释放后清空 | 与劈棍 1 星相同 |
| 蓄力重击(2 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 8.8 | 默认 | 默认 | 默认 | 释放后清空 | 与劈棍 2 星相同 |
| 蓄力重击(3 星) | 按住重击键蓄力, 松手释放 | 释放消耗所有星数与棍势条 | 11.0 | 默认 | 默认 | 默认 | 释放后清空 | 与劈棍 3 星相同 |
| 蓄力重击(4 星) | 满 4 星按重击键**直接释放**, 无需蓄力 | 消耗全部棍势 | 2.0 + 2.0 + 14.0(三段) | 默认 | 默认 | 默认 | 释放后清空 | - |

> 大圣式同样有四蓄窗口(15s)、霸体减伤(重击动作期间按动画标记 50%~90%)、铜头铁臂加棍势等通用机制。

---

## 8. 法术技能(方向键施放)

三类法术槽: 身法(↑)、法术(↓)、毫毛(←)。所有法术**仅持棍时可施放**, 不消耗资源, 只有冷却。HUD 上图标半透明表示冷却中, 并显示剩余秒数。

### 8.1 聚形散气(身法槽)

| 技能 | 触发方式 | 冷却 | 持续/窗口 | 核心效果 | 附加说明 |
| --- | --- | --- | --- | --- | --- |
| 聚形散气(身法槽) | 按 ↑ 键, 冷却完毕时施放 | **700 tick(35 秒)** | 隐身持续 **200 tick(10 秒)** | 原地留下一个"筋斗云残留实体"诱饵, 复制玩家最大生命, 吸引 30 格内怪物仇恨并转移其攻击目标; 隐身中按普攻键改为破隐一击: 伤害 **x3.0**, 冲击 **x4.0**, 最多命中 1 个目标, 10 格内有怪物时使用带追踪冲刺的版本(自动向目标位移) | 隐身期间攻击/受击/攻击动画结束都会提前破隐; 施法音 + 带贴图残影 |

### 8.2 铜头铁臂(身法槽)

| 技能 | 触发方式 | 冷却 | 持续/窗口 | 核心效果 | 附加说明 |
| --- | --- | --- | --- | --- | --- |
| 铜头铁臂(身法槽) | 按 ↑ 键, 冷却完毕时施放, 进入格挡架势 | **300 tick(15 秒)** | 格挡窗口 **30 tick(1.5 秒)** | 格挡成功: 本次伤害完全取消, 进入 **30 tick(1.5 秒)无敌帧**, 并按当前棍式给重击技能 **+60 棍势**(与识破奖励相同; 跨过升星阈值则自动升星) | 立棍式额外解锁 30 tick(1.5s)直接起手窗口, 戳棍式额外解锁 30 tick(1.5s)直接释放窗口; 窗口内未被击中则播放失败动画 |

### 8.3 安身法(法术槽)

| 技能 | 触发方式 | 冷却 | 持续/窗口 | 核心效果 | 附加说明 |
| --- | --- | --- | --- | --- | --- |
| 安身法(法术槽) | 按 ↓ 键, 冷却完毕时在脚下画出火圈法阵 | **1000 tick(50 秒)** | 法阵持续 **510 tick(25.5 秒)**, 前 15 tick 为预热; 每 **5 tick(0.25s)** 脉冲一次; 法阵半径 **4.7 格** | 每次脉冲: 半径内怪物点燃 1 秒 + 径向击退(强度 0.24, 上抬 0.05); 圈内玩家**回血 1.0 点**(0.2 x 5) + **回棍势 5 点**(平均 1 点/tick) | 火圈由 48 个火焰粒子组成; 理论全程总治疗约 99 点、总回棍势约 594 点(以打满 25.5s 计算, 实际受站位影响) |

### 8.4 定身法(法术槽)

| 技能 | 触发方式 | 冷却 | 持续/窗口 | 核心效果 | 附加说明 |
| --- | --- | --- | --- | --- | --- |
| 定身法(法术槽) | 按 ↓ 键, 冷却完毕时施放; 优先定身当前锁定目标(正在闪避动画中的目标除外), 无目标时自动选取 **50 格内最近**的生物(分身/筋斗云残留除外) | **1000 tick(50 秒)** | 定身持续 **192 tick(9.6 秒)**, 倒计时结束自动解除 | 目标获得 192 tick 的"定"效果 + 发光效果, 移速清零(原速度被记录以便恢复); 怪物与末影龙额外**关闭 AI** | 解除: 时间到/技能被移除时恢复移速与 AI, 怪物解除后会立即仇恨施法者; 未锁定目标时按 64 格半径兜底搜索解除; 定身期间每 5 tick 在目标身上播放"定"字残影与粒子 |

### 8.5 身外身法(毫毛槽)

| 技能 | 触发方式 | 冷却 | 持续/窗口 | 核心效果 | 附加说明 |
| --- | --- | --- | --- | --- | --- |
| 身外身法(毫毛槽) | 按 ← 键, 冷却完毕时施放 | **2400 tick(120 秒)** | - | 生成 **7 个**假悟空, 以玩家为圆心 **5 格半径**环形生成, 伴随烟雾粒子; 分身复制玩家的手持武器与全套盔甲, 最大生命 += 玩家最大生命, 移速 0.4, 跟随玩家并攻击玩家的目标 | 分身免疫来自玩家/其它分身的伤害与摔落伤害, 死亡不掉落装备; 玩家使出普攻 5 段或任意重击时, 距目标 4 格内的分身会看向目标并**同步播放相同攻击动画**; 玩家不会受到自己或自己分身的伤害 |

---

## 9. 数值出处(代码/配置来源)

| 内容 | 出处文件 |
| --- | --- |
| 技能注册与动画装配 | `src/main/java/com/p1nero/wukong/epicfight/skill/WukongSkills.java` |
| 按键定义与施放请求 | `.../client/keymapping/WukongKeyMappings.java` |
| 劈棍重击/破棍/斩棍/跳重击/识破数值 | `.../skill/custom/wukong/SmashHeavyAttack.java` |
| 立棍重击/风云转/江海翻 | `.../skill/custom/wukong/PillarHeavyAttack.java` |
| 戳棍重击/退寸/进尺/搅棍/凤穿花/击退点燃 | `.../skill/custom/wukong/ThrustHeavyAttack.java` |
| 大圣式衍生/蓄力 | `.../skill/custom/wukong/GreatSageHeavyAttack.java` |
| 闪避与完美闪避 | `.../skill/custom/wukong/WukongDodgeSkill.java`, `.../animation/custom/WukongDodgeAnimation.java` |
| 棍花格挡/回棍势 | `.../skill/custom/wukong/StaffPassive.java` |
| 聚形散气 | `.../skill/custom/fashu/ShenfaJuxingsanqiSkill.java`, `.../entity/CloudStepLeftEntity.java` |
| 铜头铁臂 | `.../skill/custom/fashu/ShenfaTongtoutiebiSkill.java` |
| 安身法 | `.../skill/custom/fashu/FashuAnshenfaSkill.java` |
| 定身法 | `.../skill/custom/fashu/FashuDingshenfaSkill.java`, `.../skill/custom/BattleUnit.java` |
| 身外身法 | `.../skill/custom/fashu/ShenWaiShenFaSkill.java`, `.../entity/FakeWukongEntity.java`, `BattleUnit.fenshen` |
| 各招式伤害/冲击/硬直倍率 | `.../animation/WukongAnimations.java`, `.../animation/WukongGreatSageAnimations.java` |
| 普攻连段表与武器固有技能绑定 | `.../weapon/WukongWeaponCapabilityPresets.java` |
| 棍势上限 120 / 4 星 / 闪避无消耗 | `src/main/resources/data/wukong/skill_parameters/*.json` |
| 武器破甲/冲击/连击数(金箍棒破甲 20/亢金 8) | `src/main/resources/data/wukong/capabilities/weapons/*.json` |
| 蓄力速度/切手技窗口/四蓄窗口等可调参数 | `src/main/java/com/p1nero/wukong/Config.java` |
| 技能中文名与说明 | `src/main/resources/assets/wukong/lang/zh_cn.json` |

---

## 10. 本次改动说明(按文档规范)

- **变动功能点**: **体力/耐力值去除**。是什么: 移除模组中所有对 Epic Fight 体力(耐力)的消耗与回复逻辑, 并删除相关配置项。为什么: 让本模组与体力机制完全无关, 招式只受棍势与冷却约束。作用: 闪避/棍花/搅棍不再因体力不足而受限, 完美闪避/退寸/铜头铁臂/戳棍命中也不再回复体力。
- **修改前情况**: 悟空闪避消耗 2.0 体力且完美闪避回 3.0 体力; 棍花每 tick 消耗 0.1 体力、体力不足自动停止; 戳棍命中回 2 体力、退寸赌胜回 5 体力、搅棍体力不足自动收尾; 铜头铁臂格挡成功回 5 体力; 配置中含 `staff_flower_stamina_consume` 与未使用的 `derive_stamina_consume` 两项。**修改后情况**: 上述体力消耗/回复/校验全部移除, 闪避技能资源类型改为无消耗(`Resource.NONE`), `dodge.json` 消耗改为 0.0, 两个体力配置项删除。**区别**: 共修改 6 个 Java 文件(`WukongDodgeSkill`/`StaffPassive`/`ThrustHeavyAttack`/`ShenfaTongtoutiebiSkill`/`PlayStaffFlowerPacket`/`Config`)与 1 个参数文件(`dodge.json`), 并同步更新本文档; 除体力相关行为外, 招式伤害/棍势/冷却等数值不变。
- **变动功能点**: **文档表格字段统一**。是什么: 将第 1~7 章原本 8 种不同列结构的招式表统一为固定 9 列(`招式 | 触发方式 | 消耗/前提 | 伤害倍率 | 冲击 | 命中目标数 | 硬直 | 棍势变化 | 附加效果`), 第 8 章法术表统一为固定 6 列(`技能 | 触发方式 | 冷却 | 持续/窗口 | 核心效果 | 附加说明`), 并在 0.1 节新增表格字段说明。为什么: 原文档各表列数与列含义不一致(2 列"效果/数值"表、星数横排的转置表、3~6 列混合表并存), 无法逐列对比。作用: 所有招式按同一字段口径排列, 无内容的格子统一填 `-`, 便于横向对比伤害/冲击/硬直/棍势变化等数值。**修改前情况**: 普攻表 4 列、大圣普攻表 3 列(缺触发)、闪避/棍花/切手技/法术表为 2 列"效果/数值"、劈棍蓄力表 6 列、斩棍式与跳跃重击为星数横排的转置表。**修改后情况**: 上述表格全部按固定字段重写, 斩棍式/跳跃重击等转置表改为按星数逐行排列。**区别**: 仅调整表格结构与排版, **所有数值内容未做任何改动**; 第 0 章说明性表格(概念/按键/配置)与第 9 章出处表因性质不同保留原格式。
- **变动功能点**: **连段间隔统一为 30 tick(1.5 秒)**。是什么: 将模组中所有数值不一致的连段/派生判定窗口统一改为 30 tick。为什么: 原各窗口 16/18/20/25/30 tick 并存, 手感不统一且难以记忆。作用: 普攻连段、大圣式一段/二段衍生、铜头铁臂格挡及其直接起手/释放窗口全部与其它切手技窗口(30 tick)一致。**修改前情况**: 普攻连段间隔判定 16 tick(0.8s, `Config.basic_attack_time_interval` 默认值); 大圣式一段衍生窗口 25 tick(`GreatSageHeavyAttack` `CAN_FIRST_TIMER`); 大圣式二段衍生窗口 20 tick(`CAN_SECOND_TIMER`); 铜头铁臂格挡窗口 18 tick(`TTTB_RESTORE_TIMER`); 铜头铁臂成功后立棍直接起手窗口 18 tick(`PILLAR_FASHU_TIMER`); 戳棍直接释放窗口 20 tick(`THRUST_FASHU_TIMER`)。**修改后情况**: 上述 6 处全部改为 30 tick(1.5s)。**区别**: 共修改 5 个 Java 文件(`Config`/`GreatSageHeavyAttack`/`ShenfaTongtoutiebiSkill`/`PillarHeavyAttack`/`ThrustHeavyAttack` 中的 6 处赋值, 其中 Config 仅改默认值), 并同步更新本文档 0.3/7/8.2 节; 普攻手感从 0.8s 放宽到 1.5s, 大圣式衍生与铜头铁臂窗口相应放宽, 其余数值不变。未改动项: 闪避连段重置 100 tick(语义为"多久不闪回第 1 段", 非连段间隔)与各类冷却/持续时间保持不变。
- **风险与规避**: 1) 旧存档/旧配置文件中残留的 `staff_flower_stamina_consume`、`derive_stamina_consume` 键会被 Forge 配置加载器忽略, 不影响运行, 如需彻底清理可删除 `config/wukong-common.toml` 让其重新生成。2) 游戏内体力条仍存在(Epic Fight 本体机制), 但本模组不再读写它, 属正常现象。3) 已通过 `./gradlew compileJava` 编译验证无错误。4) 表格统一为纯文档改动, 不影响游戏运行; 若后续代码数值变动, 需按固定字段同步更新表格, 避免再次出现列口径不一致。5) `basic_attack_time_interval` 为 Forge 配置项, **已存在的 `config/wukong-common.toml` 中仍是旧值 16.0**, 本次只改了默认值; 如需对旧存档生效, 请删除该配置文件让其以新默认值 30.0 重新生成, 或手动改为 30.0。
- **出处**: 见第 9 节表格; 体力相关原逻辑出自 `WukongDodgeSkill.modifyStamina`、`ThrustHeavyAttack.modifyStamina`/`hasStamina`、`ShenfaTongtoutiebiSkill.modifyStamina`、`StaffPassive.hasStamina`、`PlayStaffFlowerPacket.setStamina`、`Config.STAFF_FLOWER_STAMINA_CONSUME`/`DERIVE_STAMINA_CONSUME`(均已移除); 表格数值内容出处同样见第 9 节, 仅重排未改值; 连段间隔统一改动出处为 `Config.BASIC_ATTACK_INTERVAL_TICKS`(默认值 16.0→30.0)、`GreatSageHeavyAttack`(`CAN_FIRST_TIMER` 25→30 / `CAN_SECOND_TIMER` 20→30)、`ShenfaTongtoutiebiSkill`(`TTTB_RESTORE_TIMER` 18→30)、`PillarHeavyAttack`(`PILLAR_FASHU_TIMER` 18→30)、`ThrustHeavyAttack`(`THRUST_FASHU_TIMER` 20→30)。

## 11. 已知遗漏与注意事项

1. **伤害为倍率口径**: 实际伤害 = 武器面板攻击 x 倍率, 并叠加 Epic Fight 的破甲(金箍棒 20/亢金 8)、冲击(0.6)等属性; 面板攻击取决于武器(金箍棒为下界合金等级 +3, 即约 7 点; 亢金 +1; 测试棍 -3)。
2. 立棍 2/3/4 星重击与劈棍 2/3/4 星重击的**减伤霸体数值写在动画事件里**(50%/70%/90%), 两种棍式共用 `DAMAGE_REDUCE` 数据键; 若未来动画调整需重新核对。
3. 戳棍 `ThrustHeavyAttack` 中存在对"风云转命中时击退强度 2"的判定(引用了立棍动画), 疑似跨棍式复用代码, 已按代码原样记录。
4. 闪避技能参数文件中含 `xp_requirement: 114514`(彩蛋占位值), 因闪避已绑定棍武器自动装配, 该值无实际影响。
5. 大圣式在代码中通过按键 G 直接切换, 未强制校验大圣套装(套装仅解锁劈棍的三段衍生 HENSHIN), 与游戏内文案提示可能不同, 以代码为准。
