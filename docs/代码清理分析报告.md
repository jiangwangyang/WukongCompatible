# WukongCompact 冗余代码清理分析报告

> 分析范围：`src/main/java/com/p1nero/wukong/` 全部 93 个 Java 文件（约 11000 行），并已在 `src/main/resources`（含 mixins.json、动画/技能 JSON）中交叉验证，避免误删被数据驱动引用的符号。
> 状态：**仅分析，未做任何修改**。请确认后我再动手。

---

## 一、可直接删除（高置信，无风险）

### 1.1 被注释掉的代码（调试残留 / 旧版本逻辑）

| # | 位置 | 内容 | 性质 |
|---|------|------|------|
| 1 | `client/AnimationJudge.java:80` | 注释掉的 LOGGER.error | 调试残留 |
| 2 | `item/client/JinGuBangRenderer.java:86` | 注释的 setShaderColor | 调试残留 |
| 3 | `network/packet/client/AddEntityAfterImageWithTextureParticle.java:32` | 旧粒子方案（已被下行 WuKongParticles 取代） | 废弃 |
| 4 | `network/packet/server/PlayStaffFlowerPacket.java:35-49` | execute() 旧分支逻辑整块，且引用作用域内不存在的变量 | 废弃 |
| 5 | `entity/CloudStepLeftEntity.java:64` | System.out.println 调试 | 调试残留 |
| 6 | `entity/FakeWukongEntity.java:74` | 注释掉的 MeleeAttackGoal 注册 | 废弃 |
| 7 | `WukongMoveset.java:59,75,77` | 注释掉的日志 / FOV 代码 / 调试命名 | 调试残留 |
| 8 | `mixin/PatchedLivingEntityRendererMixin.java:39-41` | 注释掉的"持金箍棒"守卫判断 | 废弃 |
| 9 | `mixin/animation_types_mixin/AnimationPlayerMixin.java:14-18` | tick 注入体全部注释 | 死注入点 |
| 10 | `epicfight/animation/WukongAnimations.java:494-598` | **105 行巨型注释块**：整套旧"立棍蓄力循环"动画系统（PILLAR_CHARGED_LOOP1/2/3 等）。已验证 resources 中无任何 JSON 引用这些 ID | 废弃 |
| 11 | `epicfight/skill/custom/wukong/ThrustHeavyAttack.java:133-145, 245-280` | 旧"法术栈"逻辑、旧 TAKE_DAMAGE 监听器（活跃代码已有新版） | 废弃 |
| 12 | `epicfight/skill/custom/fashu/*`（5 个技能文件） | onInitiate/onRemoved 内被注释的旧重置逻辑；`ShenWaiShenFaSkill.java:146-171` 整个方法被注释 | 废弃 |
| 13 | `epicfight/skill/custom/wukong/PillarHeavyAttack.java:164-166` | 引用**不存在的类** MoreBattlesSkillDataKeys | 迁移残留 |
| 14 | `epicfight/weapon/WukongWeaponCapabilityPresets.java:201-227` | SMASH_ONLY 旧预设 + 注释掉的注册 | 废弃 |
| 15 | 各类散落单行注释（ThrustHeavyAttack、SmashHeavyAttack:237、WukongDodgeSkill:142、WukongMoveCoordFunctions、BattleUnit 等） | 调试日志/重复声明注释 | 调试残留 |

删除 #4、#9 时需连带清理未使用的 import；删除 #9 需同步从 `src/main/resources/wukong.mixins.json` 移除 `AnimationPlayerMixin` 条目（该类仅此一个方法，可整体删除）。

### 1.2 无用的空方法 / 空类

| # | 位置 | 方法 | 说明 |
|---|------|------|------|
| 16 | `effects/DingParticle.java:50` | `tick()` 仅调 super | 冗余 override |
| 17 | `item/client/JinGuBangRenderer.java:78` | `preRender(...)` 仅调 super | 冗余 override |
| 18 | `WukongMoveset.java:103` | `onEntityDeath` 空事件处理器 | 需连带删第 64 行 addListener |
| 19 | `WukongMoveset.java:111` | `onPlayerTick` 方法体全注释 | 需连带删第 65 行 addListener |
| 20 | `epicfight/animation/custom/WukongScaleStaffAttackAnimation.java:52` | `CameraResetFov()` 体全注释，且第 95 行仍在调用（空操作） | 删方法 + 调用点 |
| 21 | `epicfight/skill/custom/guangzhi/GuangZhiDodgeSkill.java`、`GuangZhiHeavyAttack.java` | **两个空类，全项目无任何引用** | 整文件删除 |

### 1.3 未使用的变量 / 方法 / 字段

| # | 位置 | 符号 | 证据 |
|---|------|------|------|
| 22 | `client/keymapping/WukongKeyMappings.java:224` | `heavyAttackKeyPressed(int)` | 全项目零调用 |
| 23 | `mixin/PatchedLivingEntityRendererMixin.java:69,72` | `isHoldingJingubang` / `isJingubang` | 仅被注释代码"引用" |
| 24 | `mixin/PatchedLivingEntityRendererMixin.java:27` | `LOGGER` 字段 | 定义后从未使用 |
| 25 | `capability/WKPlayer.java:12,16,20` | `blfov` / `setFovsz` / `getFovsz` | 仅存在于注释中 |
| 26 | `Config.java:80-90` | 两个私有 `setConfig(...)` 重载 | 无任何调用 |
| 27 | `epicfight/animation/WukongAnimations.java:186` | `EVISCERATE_FIRST` | 仅声明，永为 null，resources 无引用 |
| 28 | `epicfight/animation/WukongGreatSageAnimations.java:59-60` | `FENG_YUN_SAO_STYLE` / `SAO_CHUO_SHI_STYLE` | 别名字段，赋值后从未读取（见"待确认 2"） |
| 29 | `epicfight/skill/custom/wukong/ThrustHeavyAttack.java:159` | `wuKongFengShenZhiLl(...)` | 活跃代码零调用 |
| 30 | `epicfight/skill/custom/fashu/ShenfaJuxingsanqiSkill.java:59,174` | `currentWeapon` 字段 / `restoreArmor()` 方法 | 从未赋值/调用（见"待确认 5"） |
| 31 | 各文件未使用 import（约 8 处：ShenWaiShenFaSkill 的 org.apache.http、JinGuBangRenderer 的 Logger 等） | | 删上述代码后一并清理 |

### 1.4 顺带发现的小缺陷（低风险修复）

- `WukongScaleStaffAttackAnimation.java:86-87`：`putBoolean("WK_shouldScaleItem", false)` 连写两次，留一行即可
- `StaffPassive.java:105-106`、`ThrustHeavyAttack.java:228-229/239-240`：`event.setCanceled(true)` 连续两次（幂等无害，疑似笔误）
- `ThrustHeavyAttack.java:405`：乱码注释 `//鎵ｈ€愬姏`（GBK/UTF-8 错位）
- `FakeWukongEntity.java:109-118`：两段几乎重复的 `getTarget() instanceof CloudStepLeftEntity` 判断块，第二段疑似复制粘贴冗余

---

## 二、不能删的（已确认有用，避免误伤）

- **设计性空 override**：`CloudStepLeftEntity.setItemSlot/getArmorSlots/getItemBySlot`（防止诱饵实体装装备导致物品复制）、`FakeWukongEntity.getBreedOffspring`（抽象方法强制实现）、`WukongArmorMaterials` 各 getter、Epic Fight 生命周期钩子 `StaffStance.onInitiate/onRemoved` 等
- **所有 mixin 类**（除 AnimationPlayerMixin 外）均被 `wukong.mixins.json` 引用，方法带 @Inject/@Redirect 注解，是字节码注入目标，不能按"无人调用"删除
- 动画 ID 常量（如 PILLAR_CHARGED_LOOP4、PILLAR_START4、WIND_CLOUD_STYLE、SWEEP_JAB_STYLE）已被活跃代码或 resources JSON 引用

---

## 三、需要你确认的项目（疑似未完成功能，删/留取决于规划）

1. **`WKPlayer.damageReduce`（死分支）**：getter 在 `PillarHeavyAttack.java:237`、`ThrustHeavyAttack.java:272` 被读取，但全项目**没有任何地方赋值**，恒为 0 → 两个 `if (getDamageReduce() > 0)` 分支永远不执行。是打算补全免伤逻辑，还是删除字段+分支？
2. **`Config.DERIVE_STAMINA_CONSUME`**：配置项会生成到 TOML 供玩家修改，但代码从不读取。接回逻辑还是删除？
3. **`Config.registerCommands` / `/wukong` 命令**：注册了一个无任何子命令的空命令壳。保留占位还是删除？
4. **`FashuAnshenfaSkill.deriveAnimation2`**：声明但从未赋值，而 `deriveAnimation1` 在用。是漏写赋值（潜在 bug）还是确认不用？
5. **`ShenfaJuxingsanqiSkill.currentWeapon` / `restoreArmor`**：语义是"聚形散气卸甲后复原护甲"，疑似未完成功能。补全还是删除？
6. **四个法术技能 Builder 的 `animationProviders` 字段**：set 了但构造器从不读取（4 处）。确认法术技能已改走事件触发动画后可删字段+setAnimations。
7. **`registerPropertiesToAnimation()` 9 处空 override**（各 HeavyAttack / 法术技能）：仅 `return this;`，与 Epic Fight 基类行为一致。删了没风险但属于 override 钩子，建议确认这些技能确实无需注册动画属性。
8. **`WukongAnimations` 旧蓄力循环注释块（第一部分 #10）**：确认没有想复用的 getScaleEvents 配置片段。

---

## 四、统计

- 被注释代码：**15 组**（基本均可删）
- 空/死方法：**6 处可直接删**，另有 9 处空 override 待确认
- 未使用符号：**10 项可删**，3 项待确认（damageReduce、DERIVE_STAMINA_CONSUME、/wukong 命令）
- 预计删除：约 **400+ 行**死代码、2 个空类、1 个 mixin 类
